namespace MerchForge.api.Enums;

/// <summary>Logo and TemplateImage share the same size cap; Favicon gets a tighter one.</summary>
public enum WebsiteCustomizationImageKind
{
    Logo,
    Favicon,
    TemplateImage
}
