# Copy to .env before running `docker compose up`. Real values, not committed
# (see .gitignore) - this local stack still needs real secrets even though
# it's not production.
DB_ROOT_PASSWORD=change-me-locally
JWT_SECRET_KEY=a-long-random-local-only-secret-at-least-32-characters
